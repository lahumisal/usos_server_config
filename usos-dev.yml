#smart_school-dev.yml
server:
  port: 9090

cors:
  allowed-origins: http://localhost:2025,http://localhost:2026
  allowed-methods: GET,POST,PUT,DELETE,OPTIONAL
  allowed-headers: "*"
  exposed-headers: Authorization
  allow-credentials: true
  max-age: 3600
